# GitHub Copilot Instructions - タスク管理システム構築

このプロジェクトは、Flask ベースのタスク管理システムです。
以下の指示に従ってメンバーをサポートしてください。

## プロジェクト概要

- **名称**: 社内庶務タスク管理システム
- **技術スタック**: Flask 3.0.3 + PostgreSQL + Bootstrap 5.3.3
- **デプロイ先**: Render
- **所要時間**: 約 60 分

## 使用可能なガイド

メンバーからの質問に応じて、以下のガイドを参照してください：

### 1. クイックスタート（最初に読むべき）
**ファイル**: `QUICK_START_GUIDE.md`
**用途**: 7 ステップで完成。シンプルな手順を望むメンバー向け

### 2. 詳細ガイド（理解を深めたい時）
**ファイル**: `DEPLOYMENT_GUIDE.md`
**用途**: 各ステップの背景説明・トラブルシューティング

### 3. 配布ガイド（全体理解用）
**ファイル**: `MEMBER_DISTRIBUTION_GUIDE.md`
**用途**: プロジェクト全体・学習目標の確認

---

## メンバーとの対話パターン

### パターン 1: 「とにかく早く始めたい」

**対応:**
```
→ QUICK_START_GUIDE.md の Step 1-3 を提示
→ ローカル環境準備のコマンドを実行してもらう
→ `bash setup.sh` または `setup.bat` を実行するよう案内
```

### パターン 2: 「エラーが発生した」

**対応:**
```
1. エラーメッセージの詳細を聞く
2. DEPLOYMENT_GUIDE.md のトラブルシューティングを検索
3. 「PostgreSQL コンパイルエラー」→ Python 3.11.0 を案内
4. Render Logs の確認を案内
```

### パターン 3: 「コードを理解したい」

**対応:**
```
→ DEPLOYMENT_GUIDE.md の「主要ファイル概要」セクション を提示
→ app.py の重要な関数を説明
→ models.py のテーブル定義を説明
→ カスタマイズの相談に乗る
```

### パターン 4: 「自分のシステムにカスタマイズしたい」

**対応:**
```
→ 既存コードの構造を説明
→ 必要な変更箇所を提示
→ 実装コードを提供
→ テスト方法をアドバイス
```

---

## 重要な設定情報

### 初期ユーザー

```
ユーザーID: admin
パスワード: admin123
役割: 管理者

ユーザーID: member01-03
パスワード: password123
役割: メンバー
```

### 必要なツール

```
- Python 3.11 以上
- Git
- GitHub アカウント
- Render アカウント（GitHub で連携）
- VS Code（推奨）
```

### ローカル実行

```bash
# セットアップ自動化
bash setup.sh          # macOS/Linux
setup.bat              # Windows

# 手動実行
python -m venv venv
source venv/bin/activate  # Windows: venv\Scripts\activate
pip install -r requirements.txt
python app.py
```

### Render デプロイ

```
1. GitHub にプッシュ
2. Render で PostgreSQL を作成
3. Render で Web Service を作成
4. 環境変数を設定（SECRET_KEY・DATABASE_URL）
5. デプロイ完了を確認
```

---

## コード生成時のガイドライン

メンバーがコード生成を求めてきた場合：

### ✅ 提供できるコード

- ✅ Flask ルート追加
- ✅ モデル拡張
- ✅ HTML テンプレート追加
- ✅ CSS スタイル調整
- ✅ JavaScript/Ajax 追加
- ✅ API エンドポイント追加

### ⚠️ 注意が必要なコード

- ⚠️ 認証・セキュリティ関連 → 既存の実装を確認してから
- ⚠️ DB マイグレーション → test してから本番適用
- ⚠️ デプロイ設定 → render.yaml を確認

### ❌ 提供すべきでないコード

- ❌ SECRET_KEY や DATABASE_URL をハードコード
- ❌ パスワードをプレーンテキストで保存
- ❌ SQL インジェクション対策なしのコード

---

## トラブルシューティングフロー

```
メンバーからの質問
    ↓
1. 詳細を聞く（エラーメッセージ、実行環境 など）
    ↓
2. DEPLOYMENT_GUIDE.md でトラブル検索
    ↓
3. 見つかった → 解決方法を提示
4. 見つからない → ネットで検索・デバッグのアドバイス
    ↓
成功
```

---

## セキュリティ上の注意

メンバーに提供する際の注意点：

- [ ] SECRET_KEY は絶対に GitHub にコミットしない
- [ ] .env ファイルは .gitignore に含める
- [ ] DATABASE_URL は Render の環境変数で管理
- [ ] 本番環境のパスワードは定期的に変更
- [ ] API キーは環境変数で管理

---

## パフォーマンス・トラブル時の確認項目

```
✅ ログインが遅い
   → Render のログで DB 接続を確認
   
✅ ステータス更新が失敗
   → ブラウザコンソール（F12）でエラー確認
   
✅ ページが表示されない
   → Render Logs でサーバーエラーを確認
   
✅ データベース接続エラー
   → DATABASE_URL が正しいか確認
   → PostgreSQL が起動しているか確認
```

---

## 回答時の形式

メンバーの質問に答える時は、以下の形式を推奨：

```
## 🎯 質問への回答

### 問題
[メンバーの質問を要約]

### 解決方法
[具体的な手順]

### コード例（必要なら）
\`\`\`python
# コード
\`\`\`

### 確認方法
[動作確認の方法]

### 参考
[関連ガイドや参考資料]
```

---

## よくある質問（FAQ）集

### Q. 無料プランで十分？
**A.** 開発・デモ用に十分。本番は有料プラン推奨。

### Q. PostgreSQL が必要？
**A.** はい。ローカルでも Render でも必須。

### Q. GitHub 認証なしでデプロイ？
**A.** 不可。GitHub にコードが必須。

### Q. 本番環境でデータは消える？
**A.** いいえ。PostgreSQL なら永続。

### Q. カスタマイズは自由？
**A.** はい。ただしセキュリティに注意。

---

## 状況別アドバイス

### 😕 「よく分からない」

```
→ QUICK_START_GUIDE.md に戻る
→ Step 1-3 をコピー&ペースト で実行
→ `setup.sh` / `setup.bat` で自動化
→ ローカル起動確認まで進める
```

### 🚨 「エラーで止まった」

```
→ エラーメッセージを Copilot に貼り付け
→ DEPLOYMENT_GUIDE.md のトラブル検索
→ 同じエラーがあれば解決方法を提示
→ 見つからなければネット検索をアドバイス
```

### 💪 「もっと学びたい」

```
→ DEPLOYMENT_GUIDE.md を全部読むよう案内
→ コード例を提供してカスタマイズ練習
→ Flask/PostgreSQL/Bootstrap の公式ドキュメントを紹介
```

---

## 最後のチェック

メンバーがこれを読んだら、以下を確認してください：

- [ ] 環境は準備できたか？
- [ ] ローカルで起動できたか？
- [ ] ログインでき たか？
- [ ] 次のステップ（GitHub・Render）は理解できたか？

すべて OK なら、Render デプロイに進んでください！🚀

---

**このプロジェクトが成功することを祈っています！** 🙏
